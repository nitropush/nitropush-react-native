///
/// HybridLocalPackageSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/// See ``HybridLocalPackageSpec``
public protocol HybridLocalPackageSpec_protocol: HybridObject {
  // Properties
  var releaseId: String { get }
  var label: String { get }
  var packageHash: String { get }
  var packageSize: Double { get }
  var appVersion: String { get }
  var otaVersion: Double? { get }
  var displayVersion: String? { get }
  var platforms: [String]? { get }
  var isMandatory: Bool { get }
  var description: String? { get }
  var isPending: Bool { get }
  var isFailedInstall: Bool { get }
  var isFirstRun: Bool { get }
  var bundlePath: String { get }

  // Methods
  func install(installMode: InstallMode, minimumBackgroundDuration: Double) throws -> Promise<Void>
  func rollback() throws -> Promise<Void>
}

public extension HybridLocalPackageSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject LocalPackage]"
  }
}

/// See ``HybridLocalPackageSpec``
open class HybridLocalPackageSpec_base {
  private weak var cxxWrapper: HybridLocalPackageSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridLocalPackageSpec_cxx {
  #if DEBUG
    guard self is any HybridLocalPackageSpec else {
      fatalError("`self` is not a `HybridLocalPackageSpec`! Did you accidentally inherit from `HybridLocalPackageSpec_base` instead of `HybridLocalPackageSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridLocalPackageSpec_cxx(self as! any HybridLocalPackageSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the LocalPackage HybridObject.
 * Implement this protocol to create Swift-based instances of LocalPackage.
 * ```swift
 * class HybridLocalPackage : HybridLocalPackageSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridLocalPackageSpec = HybridLocalPackageSpec_protocol & HybridLocalPackageSpec_base
